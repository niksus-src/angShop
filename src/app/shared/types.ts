export type User = {
  email: string;
  password: string;
  returnSecureToken: boolean;
};

export type Product = {
  info: string;
  photo: string;
  price: string;
  title: string;
  type: string;
  date: string;
};

export type FbRes = {
  name: string;
};
